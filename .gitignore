# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
jscpd-report

# Secrets / env (skills.sh OIDC, Infisical exports, vercel env pull)
.env
.env.*
!.env.example
!.env.*.example
*.local
*.local.*
*.pem
*.key
*.p8
*.p12
credentials.json
**/credentials.json
infisical-export*
secrets.json
**/secrets.json

# Vercel CLI (`vercel link`) — project auth / org metadata
.vercel

# Supabase CLI link metadata
supabase/.temp

# Local catalog probes / scratch output
.tmp-probe/
tmp-probe/
probe-output/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/graphify-out
.env*

/samples

# Mobile icons from `tauri icon` (desktop-only app)
src-tauri/icons/android/
src-tauri/icons/ios/
